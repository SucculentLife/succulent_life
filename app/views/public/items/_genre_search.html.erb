<table class="table">
  <thead>
    <tr>
      <th>種類から探す</th>
    </tr>
  </thead>
  <tbody>
    <% genres.each do |genre| %>
      <tr>
        <td>
          <%= link_to genre.name, items_path(genre_id: genre.id), class: "link small" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>